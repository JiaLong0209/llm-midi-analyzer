# Environment variables
.env

# Local environment variables
.env.*.local
.env.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Poetry
.venv
poetry.lock
# (Keep poetry.lock in git if it's an application, exclude if it's a library)
# For this project, we'll keep it as it's an app.

